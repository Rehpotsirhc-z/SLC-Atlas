#+title: Benchmarks

We benchmarked AtlasForge on the 462-gene SLC atlas, which is a 5.28 GiB dataset
when all optional source files are mirrored locally.

*Note*: All serving and genome-browser benchmarks ran on the same computer using
locally downloaded data. The results compare the tools without internet latency
or differences between hosting providers.

* Key results
- Genome browser :: AtlasForge renders the full view in 1.0–1.3 s from nginx,
  close behind igv.js (0.7–1.0 s) and about three times faster than JBrowse 2 in this test.
- Genomic data :: Across the tested ranges, genome views read only 90–300 KiB
  from a 309 MiB coverage file. This avoids unnecessary downloads and keeps load times fast.
- Dynamic serving :: Even without a static export, ~atlasforge serve~ returns the
  largest JSON endpoint in 42 ms so the atlas remains responsive.
- Preparation :: The complete process of fetching, building, and exporting the SLC atlas
  takes about 15 minutes. Since it is one of the largest human gene superfamilies,
  smaller families should generally finish faster depending on internet connection.
- Static serving :: nginx handles 18–66 times as many requests per second as the
  dynamic server, making it the better option for public or high-traffic deployments.
- Hosting :: Core atlas content uses 61 MiB and a full local mirror uses 5.28
  GiB, allowing deployments to reduce storage by streaming optional data from
  source services.

* Serving
AtlasForge can generate responses on demand through its dynamic application
server or export static files for a web server like nginx to serve.

| Endpoint                    | Payload (KiB, identity / gzip) | Dynamic server requests/s | nginx requests/s |
|-----------------------------+--------------------------------+---------------------------+------------------|
| =capabilities.json=           | 0.1 / 0.1                      |                      2022 |           119016 |
| =structure/{gene}.json=       | 6.5 / 2.8                      |                      1398 |            92616 |
| =browser/tracks.json=         | 49 / 2.5                       |                       712 |            27546 |
| =genes.json=                  | 322 / 63                       |                       339 |             6108 |
| =clustering/aa_sequence.json= | 130 / 17                       |                       309 |            14282 |
| =structure.json=              | 387 / 31                       |                       173 |             4891 |
| =expression/brain.json=       | 757 / 114                      |                        60 |             2366 |
| =expression/all.json=         | 1495 / 256                     |                        25 |             1348 |
| =conservation.json=           | 3133 / 258                     |                        20 |              477 |
| *Average*                     |                                |                       562 |            29850 |
#+TBLFM: @>$3=vmean(@2..@-1);%.0f::@>$4=vmean(@2..@-1);%.0f

Throughput is the mean number of requests served per second with ten concurrent
connections. The nginx static deployment handles 18–66 times as many requests as the
dynamic server because it returns prebuilt files instead of generating each
response on demand, making the static export the better choice for public or
high-traffic deployments.

* Genome browser
** Render time
#+CAPTION: igv.js, AtlasForge, and JBrowse 2 loading ~chr1:43,510,559-44,510,559~ on cold start
[[file:../scripts/benchmarks/results/loading-comparison.gif]]

Each browser rendered the same 33 coverage tracks, gene models, and
17.4-million-variant GWAS dataset from a local server. JBrowse 2 and igv.js
used equivalent BED12 bigBed and -log10(p) bigWig files because they
cannot interpret AtlasForge's native track metadata. Values are milliseconds to a
visually stable view, shown as *cold / warm*.

| Visible region span (Mb) | igv.js (ms) | AtlasForge nginx server (ms) | AtlasForge dynamic server (ms) | JBrowse 2 (ms) |
|--------------------------+-------------+------------------------------+--------------------------------+----------------|
|                     0.05 | 652 / 599   | 982 / 899                    | 1149 / 871                     | 3380 / 3266    |
|                        1 | 824 / 734   | 1210 / 933                   | 1352 / 989                     | 4016 / 3810    |
|                       10 | 978 / 868   | 1252 / 1045                  | 1414 / 1089                    | 3537 / 3394    |
|                       50 | 1021 / 741  | 1220 / 1141                  | 1470 / 1118                    | 4107 / 3843    |
|                      249 | 740 / 674   | 1306 / 1186                  | 1451 / 1099                    | 3682 / 3467    |

Across spans from 50 kb to the full length of chromosome 1 (249 Mb), the
AtlasForge static deployment loads in 1.0–1.3 s, a little behind igv.js
(0.7–1.0 s) and about 3 times faster than JBrowse 2. The dynamic server takes
roughly 0.1–0.2 s longer than static deployment.

** Coverage-track transfer
The values show transferred data / HTTP requests for one 309 MiB bigWig track.

| Visible region span (Mb) | igv.js (KiB / requests) | AtlasForge (KiB / requests) | JBrowse 2 (KiB / requests) |
|--------------------------+-------------------------+-----------------------------+----------------------------|
|                     0.05 | 25 / 11                 | 96 / 7                      | 768 / 3                    |
|                        1 | 128 / 11                | 299 / 14                    | 1280 / 5                   |
|                       10 | 249 / 11                | 256 / 11                    | 1024 / 4                   |
|                       50 | 68 / 9                  | 92 / 6                      | 1280 / 5                   |
|                      249 | 32 / 9                  | 155 / 11                    | 933 / 4                    |

AtlasForge loads all 33 lanes concurrently, so its per-track result varies by about 30% as
prefetch requests are scheduled. igv.js and JBrowse 2 were stable between runs.

* Atlas preparation and deployment
** Preparation time
| Operation         | Time     | Work performed                                      |
|-------------------+----------+-----------------------------------------------------|
| Source fetch      | 12.5 min | Download the source data                            |
| ~gene_tables~       | 0.23 s   | Convert gene and transcript tables to Parquet       |
| ~conservation~      | 0.39 s   | Build the ortholog matrix and species tree          |
| ~structure~         | 1.25 s   | Build metadata and copy the structure models        |
| ~expression~        | 1.82 s   | Reshape the GTEx expression matrix                  |
| ~clustering~        | 11.9 s   | Run protein and codon alignments                    |
| ~browser~           | 81.7 s   | Build genomic indexes and copy the coverage data    |
| *Complete build*    | *97 s*     | Build data for all views                            |
| *Static export*     | *5.5 s*    | Write responses and copy the binary data            |
| *Total preparation* | *14.2 min* | Fetch sources, build all views, and export the site |

Source fetch time depends on network conditions and upstream availability. The GTEx expression
matrix accounts for most of the download.

** Hosting footprint
| Deployment                           | Size (MiB) |
|--------------------------------------+------------|
| Core atlas content                   |         61 |
| Remote coverage and structure models |       1116 |
| Remote coverage                      |       1874 |
| Full local mirror                    |       5407 |

| Component                |          Size (MiB) |
|--------------------------+---------------------|
| Coverage bigWigs         |                3533 |
| GWAS bigBed              |                1065 |
| Structure models         |                 750 |
| Transcript models        |                  31 |
| JSON and Parquet content |            About 23 |
| Frontend                 | 6.5; 1.5 compressed |

Coverage tracks can stream from ENCODE, and structure models can stream from AlphaFold DB or
RCSB. The GWAS and transcript-model files are hosted with the atlas.

** Software footprint
| Installation | Download (MiB) | Installed (MiB) |
|--------------+----------+-----------|
| ~atlasforge~ | 118 | About 445 |
| ~atlasforge[pipeline]~ | 172 | About 665 |

The AtlasForge package itself is 0.37 MiB. Most installed space is used by Polars, PyArrow,
SciPy, NumPy, and Biopython.

* Structure files
| Model               | Size (KiB) | Local mirror (ms) | Remote source (ms) |
|---------------------+------------+-------------------+--------------------|
| AlphaFold DB Q8WV83 |        200 |                 1 |                 61 |
| RCSB PDB 12oy       |        369 |                 1 |                 47 |

The local mirror primarily provides offline access and independence from source services.
Coordinate transfer is a small part of Mol* viewer startup in either mode.

* Scaling
The annotation, expression, and conservation build steps scale approximately linearly with the
number of genes.

|   Genes | Build time (s) | =genes.json= (KiB) | =conservation.json= (KiB) | Conservation latency (ms) |
|---------+----------------+------------------+-------------------------+---------------------------|
|      25 |           0.64 |               18 |                     170 |                       2.8 |
|     100 |           0.82 |               70 |                     680 |                      10.5 |
|     250 |           1.22 |              173 |                    1700 |                      20.7 |
|     462 |           1.95 |              322 |                    3133 |                      41.8 |
| *Average* |           1.16 |            145.8 |                  1420.8 |                      18.9 |
#+TBLFM: @>$2=vmean(@2..@-1);%.2f::@>$3=vmean(@2..@-1);%.1f::@>$4=vmean(@2..@-1);%.1f::@>$5=vmean(@2..@-1);%.1f

* Methods
Tests ran on Linux with an Intel Core Ultra 7 255HX, 30 GiB RAM, Python 3.12,
Polars 1.43, Node 20, Playwright 1.62 with Chromium, autocannon 8, nginx 1.24,
igv.js 3.8.5, and JBrowse 2 web 4.3.0.

Browser comparisons used the same data, locus, cache state, and local nginx
server. Render time is the median of three runs; other tables use at least five
runs. Remote structure timings reflect the network conditions at the time of
measurement.

See the [[../scripts/benchmarks/README.org][benchmark instructions]] for the harness, raw results, and
reproduction steps.
